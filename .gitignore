# Binaries
crust

# Database
*.db
*.db-shm
*.db-wal

# IDE
.idea/
.vscode/
*.swp

# Build
*.o
!internal/sandbox/bpf/*.o
*.exe
**/target/

# OS
.DS_Store
Thumbs.db

# BPF (machine-specific, regenerate with: bpftool btf dump file /sys/kernel/btf/vmlinux format c)
internal/sandbox/bpf/c/vmlinux.h

# VHS recordings (keep only GIF)
docs/*.webm

# Agent test local config (contains API keys)
tests/agents/config.local.yaml

# Claude settings
.claude/settings.local.json

# Local test config (contains API keys)
tests/agents/config.local.yaml
